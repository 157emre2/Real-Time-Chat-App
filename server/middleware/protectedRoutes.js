/*const express = require('express');
const router = express.Router();
const tokenAuth = require('./tokenAuth');

router.get('/chat',tokenAuth, (req, res) => {
   res.send("Token can use");
});

module.exports = router;

URL KORUMASI YAPILACAĞI ZAMAN GRAPHQL APIYE GÖRE ŞEKİLLECEK
*/